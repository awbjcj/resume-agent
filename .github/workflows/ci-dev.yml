name: CI (dev quick checks)

on:
  pull_request:
    branches: [dev]
  push:
    branches: [dev]

permissions:
  contents: read

jobs:
  ci:
    name: ci
    uses: ./.github/workflows/_reusable-ci.yml
    with:
      full: false
